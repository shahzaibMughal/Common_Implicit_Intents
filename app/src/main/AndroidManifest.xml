<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="shahzaib.com.implicit_intents">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_CONTACTS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SendSms"
            android:label="Send Sms"/>
        <activity android:name=".SendEmail"
            android:label="Send Email"/>
        <activity android:name=".SelectAndShareImage"
            android:label="Select &amp; Share Image"/>
        <activity android:name=".CapturePhoto"
            android:label="Capture Phote"/>
        <activity android:name=".SelectContact"
            android:label="Select Contact"/>
    </application>

</manifest>